# ── Portal frontend config ───────────────────────────────────────────────────
# Copy this file to `.env`. No secrets here — the Facilio API key lives in the
# gateway (../gateway/.env), never in the browser.

# Base URL of the gateway/BFF. Local dev default is http://localhost:8787.
VITE_BFF_BASE_URL=http://localhost:8787
